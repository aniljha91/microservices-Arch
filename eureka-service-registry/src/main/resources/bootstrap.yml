spring:
  application:
    name: service-registry
  profiles:
    active: local
     
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user    
 