spring:
  application:
    name: service-registry
  cloud:
    config:
      uri: http://configserver.us-west-2.elasticbeanstalk.com
      fail-fast: true
      password: password
      username: user