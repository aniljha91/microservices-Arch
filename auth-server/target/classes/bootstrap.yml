spring:
  application:
    name: auth-server
  cloud:
    config:
      uri: http://config:8889
      fail-fast: true
      password: password
      username: user
jwt:
  secret: testingJWTAuthentication
  expiration: 2
  header: Bearer