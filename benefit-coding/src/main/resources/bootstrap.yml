spring:
  application:
    name: benefit-coding

  cloud: 
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user
      
management:
  security:
    enabled: false

jwt:
  secret: testingJWTAuthentication
  expiration: 2
  header: Bearer