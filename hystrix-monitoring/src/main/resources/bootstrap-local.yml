spring:
  application:
    name: hystrix-monitoring
  profiles: 
     actibe: local 
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user