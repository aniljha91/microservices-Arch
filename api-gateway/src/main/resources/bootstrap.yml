spring:
  application:
    name: api-gateway

  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user
jwt:
  secret: testingJWTAuthentication
  expiration: 2
