spring:
  application:
    name: zipkin-server
  profiles: 
     active: local 
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user
