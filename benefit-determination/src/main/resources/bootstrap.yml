spring:
  application:
    name: benefit-determination

  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: password
      username: user
      
management:
  security:
    enabled: false